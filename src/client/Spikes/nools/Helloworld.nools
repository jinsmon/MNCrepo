define Message {

    value : 0,
    constructor : function(number){
    //console.log("rule Hello::"+message);
        this.value = number;
    }
}

//find any message that starts with hello

rule Number {
      when {
        n:Message isNumber(n.value)
      }
      then {
         console.log("Validated");
      }

}

