import { Dispatcher } from 'flux';

class AppDispatcher extends Dispatcher {

	/*
	 * Dispatcher receives the action and dispatches the payload to store. A
	 * custom
	 */
	
 

}
module.exports = new AppDispatcher();
